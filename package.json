{
  "name": "etsy",
  "scripts": {
    "build": "cd client yarn install && yarn build",
    "deploy": "cd .. && cp -a client/build/. public/",
    "heroku-postbuild": "yarn build && yarn deploy"
  },
  "dependencies": {},
  "devDependencies": {
    "@rails/webpacker": "4.3.0",
    "jquery": "^3.5.1",
    "webpack-cli": "^4.5.0",
    "webpack-dev-server": "^3.11.2"
  }
}
